# BrandAI - Environment Variables (Google Cloud API Stack)

# --- Google Cloud Configuration ---
# This is the standard method for authenticating Google Cloud services.
# 1. Create a service account in your Google Cloud project.
# 2. Grant it the "Vertex AI User" and "Cloud Vision AI User" roles.
# 3. Download the JSON key for the service account.
# 4. Set the value of this variable to the absolute path of that downloaded JSON file.
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# Your Google Cloud Project ID.
GCP_PROJECT_ID=your-gcp-project-id-here

# The Google Cloud region for your project (e.g., us-central1).
GCP_LOCATION=us-central1

# --- Backend Configuration (Optional) ---
BACKEND_PORT=8000
BACKEND_HOST=0.0.0.0